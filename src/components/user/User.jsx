import React, { useState, useEffect } from "react";
import useAxiosPrivate from "../customHooks/useAxiosPrivate.js";
// import useRefreshToken from "../customHooks/useRefreshToken.js";

const User = () => {
  return (
    <div>
      <h2>All Users</h2>

      {/* <button onClick={() => refresh()}>refresh</button> */}
    </div>
  );
};

export default User;
